# tui-do — session continuity, 2026-08-27 18:55 EDT

Supersedes `2026-08-25-1247-continuity.md`. **The project was renamed from
`criax` to `tui-do` today. Phase 4 is complete except its quick keys, and every
manual check in `md/MANUAL-CHECKS.md` now passes.**

Read in this order: `PLAN.md` for phases and scope, `CLAUDE.md` for the rules
*and* the wire-format findings (still the most expensive knowledge in the repo),
then `md/2026-08-25-phase-4-design.md` for the decisions this phase is
executing. `md/MANUAL-CHECKS.md` is the list of things a green suite has never
caught, and it grew by four entries this session.

## The rename — read this first

The project was called `criax` until 2026-08-27. Everything moved: package,
binary, four crates, module paths, environment variables (`CRIAX_*` →
`TUI_DO_*`), config and store directories, docs, scripts. The checkout moved
from `~/code/criax` to `~/code/tui-do`. The interface crate is `tui-do-ui`, not
`tui-do-tui` — the latter was the mechanical result of the rename and read
badly.

**`cria` — no `x` — is a different thing.** It is the predecessor this project
replaces, still checked out at `../cria`, and it is named throughout `PLAN.md`
and `CLAUDE.md` on purpose. The rename anchored every pattern on `criax` so none
of those mentions moved. `tui-do migrate` still imports a cria config. Do not
"finish" the rename by touching them.

## Current state — 79 commits on `main`, 460 tests passing, clippy clean

| crate | src | tests |
|---|---|---|
| `tui-do-api` | 4,253 | 1,803 |
| `tui-do-core` | 7,766 | 928 |
| `tui-do-ui` | 6,833 | 2,160 |
| `tui-do` (binary) | 1,201 | — |

Phases 0, 0.5, 1, 2 and 3 are done and signed off. Phase 4 is one step from
complete.

## Phase 4, as it stands

**Built and verified against dev, by hand, on the release binary:**

| key | does | notes |
|---|---|---|
| `a` | add a task | status-line prompt, parsed live |
| `d` | toggle done | |
| `x` | delete | no confirmation; `u` re-creates, with a **new id** |
| `e` | edit | full form: title, description, priority, due, project, labels |
| `u` / `C-r` | undo / redo | unlimited within the session, cleared on exit |
| — | `tui-do add 'text'` | same parser, same builder, same outbox |

**Not built — this is the whole of what is left in Phase 4:**

```
p   priority        D   due date
l   labels          m   move to project
Space  configured quick actions
```

`config::QuickActionKind` is fully parsed and `migrate.rs` converts them out of
a cria config *with tests*, but nothing in the UI ever reads them — a user
migrating from cria today has their quick actions silently dropped.

Suggested order: `p` and `D` first (single-value edits, they establish the
pattern), then `m`, then `l` (multi-select, and it needs the attach/detach
mutation split the edit form already worked out), then `Space` last because it
composes the others. All of it routes through mutations that already exist —
`UpdateTask`, `AttachLabel`, `DetachLabel` — so it is keymap, a small picker and
`update` arms, not new sync machinery. `../cria/QUICK_ACTIONS.md` is the
behavioural spec.

## What driving it found this session

Every one of these was a live defect on the release binary, found by hand after
a green suite. The pattern holds: the bugs are in what the tests do not simulate
— two processes, a real terminal, a second project with the same name.

| symptom | cause |
|---|---|
| An edited task vanished from the interface *and* the web UI, having saved perfectly | The edit form showed the project by **name** and re-resolved it on save. Dev has two projects called `Inbox`; an untouched field resolved to the wrong one and moved the task. A name is a label, not a key — the field now resolves only when it was actually retyped |
| Terminal said `4 queued`, interface said `3` | `status.queued` was only ever set from a sync report, never read from the store. The outbox is *shared* — `tui-do add` in another terminal writes to the same one |
| Enter in the description did nothing visible | `rows::wrap` used `split_whitespace`, and `\n` is whitespace. The break was stored and then drawn as a space. Also fixed a multi-paragraph description rendering as one blob in the preview |
| No way to see where you were typing in the form | It drew no cursor at all. A comment claimed the focused label made one unnecessary — true for *which field*, useless for *where in it* |
| `tui-do` reported "command not found" after the rename | `~/.local/bin/tui-do` was a **dangling** symlink to the pre-rename path. A dangling link in `PATH` is indistinguishable from never having installed it |
| The first push to a new Forgejo repo was skipped | `git ls-remote --exit-code` returns 2 for a repo that answers but holds no refs — precisely a freshly created one |

## Measurements worth not repeating

**Vikunja does not require unique project titles.** Measured, not assumed: two
`PUT /projects` with the same title both succeeded (ids 40, 41; both deleted).
Dev's duplicate `Inbox` was resolved on 2026-08-27 — the account default moved
to `#12` and `#1` deleted, which the server refuses in the other order (`412`,
code `3012`) — but the *hazard* was never dev-only. Resolving a project by name
is unsafe anywhere, which is what `#12`-style references exist for.

**A project can now be named by id.** `#12` works wherever a project can be
written: `tui-do add +#12`, the edit form's project field, and
`view.default_project` in the config. That setting is also the target for a task
naming no project. Precedence: the project named → the one on screen → the
configured default → the old "find something called Inbox" guess.

**Sync speed is the server, not us.** 78 pages, ~1s each server-side:

| concurrency | time |
|---|---|
| 1 | 88.2s |
| **8 (current)** | **15.2s** |
| 16 | 16.2s |
| 24 | 26.4s |

The server stops parallelising at 8, so `CONCURRENT_PAGES` is already at the
sweet spot and raising it hurts. **The remaining win is not fetching
everything:** `filter=updated > '2026-08-25T00:00:00Z'` returns **1 page, 18
results** instead of 78 pages. Feasibility is proven; the blocker is that the
pull's retain step needs the full id list to know what the server dropped, and
an incremental pull does not mention unchanged tasks. Proposed design, **not yet
decided by the user**: `r` = push + incremental pull, with the full reconcile
kept for startup and the periodic timer. It touches the sync invariant
`CLAUDE.md` is explicit about, so it is the user's call.

## Where the code lives now

    local  --push-->  Forgejo  --Forgejo's push mirror, 8h-->  GitHub (private)

| | |
|---|---|
| **checkout** | `~/code/tui-do` |
| **Forgejo** (`origin`) | `ssh://git@sw-hp2.tail9803a5.ts.net:2222/swasko/tui-do.git` — web UI `https://sw-hp2.tail9803a5.ts.net:9443` (v15.0.7, tailnet only) |
| **GitHub** | `github.com/sjwasko/tui-do` — private, **written only by Forgejo** |

**There is no `github` remote in this checkout, on purpose.** Forgejo mirrors to
GitHub itself, which is how every other repository on that instance works —
`push_mirror` rows for `kb`, `fishin`, `cps-site`, the `tqv-*` set and the rest
all point at `github.com/sjwasko/<name>.git` on a 28800s (8h) interval, most
with `sync_on_commit`. A second path from this machine to GitHub would be a
second thing to keep in step, and the two could disagree.

Until 2026-08-27 every commit of this project existed on exactly one disk: no
remote was configured and no repository by either name existed anywhere.

`deploy/mirror-to-git.sh` pushes local → Forgejo, run by `tui-do-mirror.timer`
every eight hours (`deploy/systemd/`, symlinked into `~/.config/systemd/user/`).
**It pushes commits and never makes them** — a dirty tree is work in progress,
and committing that on a timer would write broken states into the history *and*
disguise the fact that nothing was backed up. Uncommitted paths are reported and
the run exits 2, which the unit counts as success.

`deploy/adopt-new-name.sh` moved the config, store and `PATH` symlink out of the
old name. Already run; kept because it is the record of what lives in the home
directory.

## Open items, in the order they will bite

1. **Incremental sync** — measured and designed above, undecided.
2. **`Enter` on a task** is bound to `OpenTask`, which shows the preview and
   moves focus to it. The user originally asked for a dive-into-task-detail
   screen; that is Phase 5's task screen, not this binding.

## Environment, unchanged

Dev `https://sw-surface.tail9803a5.ts.net:8443` for everything. Prod
`https://sw-hp2.tail9803a5.ts.net:8443` **read-only, always** — note that
Forgejo shares that box but is a different service on a different port, so
pushing git there is not a prod write. `~/.local/bin/tui-do` symlinks to
`target/release/tui-do`, so **build `--release` before saying a fix is ready to
try** — a debug build proves the tests pass and changes nothing the user is
looking at.

The user drives every change by hand on the release binary before it counts as
done, and that is where all six of this session's defects came from. Ship the
release build, then hand it over.

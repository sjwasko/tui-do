# tui-do — session continuity, 2026-08-24 19:15 EDT

Supersedes `2026-08-24-1822-continuity.md`. **Phase 3 is built and running.** The design
was agreed first and written down in `md/2026-08-24-1900-tui-design.md`; read that before
changing anything in `tui-do-ui`, because it records *why* each mechanism is shaped as it
is and what was tried and rejected.

`PLAN.md` is still the authority on phases and scope, `CLAUDE.md` on the rules and the
wire format.

## Current state — 42 commits on `main`, 408 tests passing, clippy clean

```
3474734 Draw it, and run it
9f98138 Give the TUI a model, a keymap and an update function
ce420eb Add session continuity notes for 2026-08-24 18:22
```

| | tests |
|---|---|
| `tui-do-api` (unit, wiremock, conformance, live) | 78 + 22 + 6 + 7 |
| `tui-do-core` unit | 150 |
| `tui-do-core` `tests/sync.rs` | 18 |
| `tui-do-ui` unit | 57 |
| `tui-do-ui` `tests/update.rs` (message sequences) | 31 |
| `tui-do-ui` `tests/render.rs` (golden files) | 15, at 45x20 through 160x50 |
| `tui-do` (the binary) | 5 |

## What is done, and what remains before Phase 3 counts as finished

Done: the model, the keymap, `update`, the theme, the row arithmetic, the renderer, and
the effect runtime. `tui-do` with no subcommand now starts the interface. Verified by
running it: it starts against an empty cache, reports in the status line why it is not
syncing, stays usable, and gives the terminal back.

**First light is done, against real data.** tui-do pulled the dev instance into
`~/.local/share/tui-do/tui-do.db`: 3,877 tasks of which 1,942 are done, 33 projects (30 real
plus the 3 pseudo-projects), 1 label — matching what the API reported in the previous
session, row for row. The sidebar draws the real tree with per-project counts, the list
shows 1,935 open tasks, and the preview pane fills.

**And the thesis holds.** Pointed at an unroutable address (`10.255.255.1`, so the
connection *hangs* rather than failing fast), tui-do renders the full cached list, accepts
motion keys, and quits cleanly while the connect is still pending. The status line says
`⟳ fetching` throughout. That is `PLAN.md`'s first non-negotiable manual check, passed.

`~/.config/tui-do/config.yaml` points at dev. Note that `tui-do migrate` would import cria's
config, which points at **prod** — and the production guard would then refuse to start,
which is the guard working.

Two things the real data showed that tests could not:

- **The preview was rendering raw HTML.** Vikunja's editor stores
  `<p><a target="_blank" rel="noopener"…`, and "show it as stored" turned out to mean
  "show markup to the user". `rows::plain_text` strips it as a stopgap; Phase 5 still
  replaces it with `glow`. That function then took a `/code-review` pass of its own and
  was rewritten around an **allowlist of known HTML elements** — see below.
- **dev has two real projects both titled `Inbox`** (id 1, empty; id 12, 16 open tasks),
  so the sidebar shows two identical rows. Not a tui-do bug — the pseudo-project `-3 Inbox`
  is correctly filtered out — but worth knowing before someone goes looking for one.

The `ubuntu:26.04` container pass is **green** — `deploy/test-ubuntu.sh`, same 377 tests,
exit 0. Still outstanding for the milestone: `/code-review` on the Phase 3 diff. The ultra
gate before Phase 3 was already run, against Phase 2's diff.

## The five things in Phase 3 that are load-bearing

Undo any of these by accident and the symptom will be baffling.

| | |
|---|---|
| **Query correlation** | Every `Effect::LoadTasks` carries a `QueryId`; an answer whose id is no longer current is *dropped*. Holding `j` down the sidebar issues a query per project and the slowest would otherwise win. `an_answer_to_a_superseded_query_is_dropped` |
| **Selection is a `TaskId`** | Never a row index. A sync that reorders or drops rows must not move the cursor onto a different task. `the_selection_follows_the_task_not_the_row` |
| **Panes are tri-state** | `Auto | Shown | Hidden`. A resize re-evaluates only panes still on `Auto`, so toggling one pins it. A pinned pane still yields below its hard minimum |
| **Modals are exclusive** | The top of the stack takes every key except `Ctrl-C`. No fall-through. `a_modal_swallows_every_key_except_quit` |
| **One keymap table** | `KEYMAP` is read by `update`, rendered by the help modal, *and* enumerated by the `:` command palette, so none of the three can drift. Adding a binding is a row, not a match arm |

## The width rule, because it will look wrong

`rows::measure` does **not** grow capped columns to their maximum first, even though
`COLUMN_LAYOUTS.md` reads that way and it is the obvious implementation. That rule hands
the labels twenty columns on a 110-wide terminal while the title stays at its minimum and
wraps to three lines. What ships: every column that can still grow grows by the same
amount, and what a capped column cannot take falls to the title. Both the documented
worked example and the narrow case come out right, and both are tested.

## Where things live

```
crates/tui-do-ui/src/
  model.rs    Model, Snapshot, Panes/PaneState, Focus, Status, Toast, landing_scope
  msg.rs      Msg          effect.rs   Effect
  update.rs   the one function that changes anything
  keymap.rs   Key, Action, Context, Group, Binding, KEYMAP, resolve
  modal.rs    Modal stack, ModalView, TextInput, fuzzy PickerState
  query.rs    Query, QueryId, Scope, order_for/sort_for
  sidebar.rs  the project tree, flattened     geometry.rs  where the panes go
  rows.rs     width arithmetic, wrapping, cell text     theme.rs  semantic colour slots
  view.rs     drawing
crates/tui-do-ui/tests/  update.rs (message sequences)  render.rs + golden/ (screens)
crates/tui-do/src/runtime/  mod.rs (the loop, effects, sync timer)  terminal.rs (the guard)
```

Re-bless the golden screens with `TUI_DO_BLESS=1 cargo test -p tui-do-ui --test render`,
then *read the diff* — they are the only test that can tell you the layout got worse.

## Deliberate omissions someone will mistake for bugs

- **`view.default_filter` is read and ignored.** It holds a Vikunja filter expression;
  the local store answers a title substring. Wiring one to the other returns nothing at
  all. Filter expressions arrive with saved filters in Phase 6.
- **`/` filters as you type**, from a prompt on the status line with a live match count.
  Enter keeps the filter, Esc restores what was showing before. Safe per-keystroke only
  because `QueryId` drops answers the user has already typed past.
- **Esc is a ladder**: modal, then focus back to the list, then clear the search. Never
  quit — that rung does not exist.
- **`:` runs a command by name**, listing the keymap with each binding's key beside it.
  Motions and the palette itself are excluded (`Action::is_command`). A command chosen by
  name takes the same code path as its key.
- **The `Table` and `Kanban` tabs draw but do nothing.** Phase 6 is the views API; the
  strip exists from Phase 3 so they land in a place rather than a redesign.
- **The preview pane is a summary, not the task.** Descriptions are HTML from the web
  editor and are shown as stored. Phase 5 pipes them through `glow` with a
  `pulldown-cmark` fallback, and adds `Screen::Task` for comments and attachments.
- **No writes.** Phase 3 is read-only by design; `Effect` is `non_exhaustive` and the
  runtime logs an unknown effect rather than dropping it silently, so Phase 4 adds write
  effects without touching this crate's shape.

## What the review changed, and why it is worth reading

`/code-review` on the description stripper found seven things, and the dev instance's own
487 non-empty descriptions settled which of them were real:

| finding | in the real data |
|---|---|
| `<` + a letter treated as a tag eats prose | **Confirmed, and worse than reported.** `<http://Www.ftc.gov>`, `<mailto:usarmy…>` and `CAM_<CAMERA_MAC>_NAME` are all real descriptions there, and the heuristic deleted each up to the next `>` — losing two URLs outright |
| numeric entities undecoded | Confirmed, though not the example given: the dev instance stores `&#x27;`, and zero `&#34;` |
| `td`/`th` missing, cells run together | 3 descriptions carry tables |
| pretty-printed markup doubles the paragraph gap | Real: `</p>\n<p>` and `</p><p>` rendered differently |
| `<script>` / `<style>` / comments shown as text | Zero occurrences — the sanitiser strips them — but `plain_text` is `pub`, so it is handled |
| per-paragraph line cap is no cap | Real: bounded now by what the pane can show |
| `<pre>` loses indentation | Real, and **accepted**: HTML whitespace collapsing is the model, and code blocks are Phase 5's job |

**366 of the 487 descriptions contain no tag at all.** That is the fact that decides the
design: this function runs mostly over prose, so "anything that looks like a tag" is the
wrong default and an allowlist of real HTML element names is the right one. A tag also has
to *end* like a tag — `<pre-release plan>` is not a `<pre>` — and text that opens a tag and
never closes it stays text rather than eating the rest of the description.

Verified by running the stripper over all 487: none left markup behind, and the 68 that
shrank sharply are links, where the `href` goes and the text stays.

## The review was acted on, and the fixes were checked rather than trusted

`0efbf2c` and `b8cab08` came from a session running in parallel. Following the precedent
set in Phase 2, they were verified rather than believed:

- **The stripper's headline finding is real, and it was my bug.** "A `<` followed by a
  letter starts a tag" deleted `<http://Www.ftc.gov>`, `<mailto:usarmy…>` and
  `CAM_<CAMERA_MAC>_NAME` up to the next `>`. Re-run over the dev instance's own 487
  non-empty descriptions, the rewrite loses none of them and leaves no element tag on
  screen. Two descriptions still come out empty: both hold nothing but a TipTap
  attachment image and an empty paragraph, so there is no text to lose — though it does
  mean an image-only description reads as "no description" until Phase 5 handles
  attachments.
- **All three `is_already_done` arms are load-bearing.** Removing the 403-on-detach arm,
  removing the attach arm, and widening it to accept any `400` code each turn exactly one
  test red, one test each.
- **The three wire answers were re-measured against dev in this session**, not taken from
  the commit message: attach-already-attached is `400` code `8001`, detach-already-
  detached is `403 Forbidden` with code `0`, and a task write echoed back the one label it
  carried. They match `CLAUDE.md` exactly.
- The prod guards are untouched — `FORBIDDEN_HOSTS`, `TUI_DO_DEV_HOST`'s allow-list
  semantics, and the binary's `PROD_HOSTS` all still read as they did.

One caution for whoever verifies the next parallel commit: a first pass here reported two
of these arms as *not* load-bearing. That was the checking harness, not the code — it
filtered failure names with `[a-z_]+`, and both test names contain digits, so it matched
nothing and printed "nothing failed". Read the raw test output.

## Verified on a phone, 2026-08-25

Termux on a Galaxy S24 Ultra, over ssh to this host. Portrait correctly refuses the
preview — there is no room for it — and landscape lays it out with the full task detail.
`z s` and `z p` both work from the soft keyboard, and scrolling was fast over the link.

That is the narrowest real terminal tui-do is likely to meet, and it is the reason the
45x20 golden files exist. Keep them.

**A native Termux build is wanted, but late** — towards the end of the process, not now.
Android/aarch64 is a fourth platform beyond `CLAUDE.md`'s Linux-through-GA policy: the
same portability rules already apply (`dirs`, no cwd-relative writes, `rusqlite` with
`bundled`), so this is a target to add, not a design to change. `bundled` SQLite matters
most here — Termux's own is not something to depend on.

## Open, and yours to close

**The dev API token used for first light was pasted into a session transcript on
2026-08-24.** Stated at the time: it is scoped to the dev instance, that instance is being
torn down after this build, and you will rotate the token. Recording it here so it is not
quietly forgotten if the teardown slips.

The token itself lives at `~/.config/tui-do/token`, mode 0600, **outside the repo**. No
token is committed anywhere in tui-do, and none should be — `server.token` exists in the
config schema but `server.token_file` is what this machine uses, and `TUI_DO_API_TOKEN`
overrides both for a single run.

The earlier open item stands unchanged: dev holds real task data behind `admin` with a
guessable password, reachable from any tailnet device, and that credential is written into
a prod task. Your call, not a task to action.

## Next

1. **Phase 4 — mutations.** Toggle done, edit, quick-add, the `Space`-prefixed quick
   actions (`QUICK_ACTIONS.md`, already parsed into `config::QuickAction`), undo/redo on
   the optimistic-write mechanism, and the write half of the pickers. The outbox, the
   rollback and the inverse-mutation machinery are all already built and tested.

**The three label questions are closed.** `./run-live-tests-token.sh` was run against dev
and all three answers are now in `CLAUDE.md`'s wire-format section, where they belong. One
of them was a live bug in shipped code: detaching an already-detached label answers
**`403 Forbidden`**, not the `404` the engine assumed, so a replayed detach undid what the
user asked for. Attaching an already-attached label answers `400` with Vikunja code `8001`
— also a 4xx, also treated as final, also rolled back. `is_already_done` now has an arm
for each, and each was verified load-bearing by reverting it and watching exactly its own
test go red.

The third answer retired an assumption rather than a bug: a task write **does** echo its
labels back, so `sync::with_labels` is belt and braces rather than the load-bearing step
its comment claimed. It stays — one measurement of one shape, guarding a silent failure —
with the comment corrected.
